// sanity.config.js
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// This file is used by Sanity's hosted studio at sanity.io/manage
// You do NOT need to embed the studio in your website.
//
// STEP 1 — Go to https://sanity.io and create a free account
// STEP 2 — Create a new project, name it "Godwa Dairy Expo"  
// STEP 3 — Copy your Project ID and paste it below
// STEP 4 — Deploy to Vercel — done!
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

// This config is for reference — Sanity reads schema from your project
// when you link it via the CLI or Sanity dashboard

export const projectId = 'tgsy9rl4'   // ← PASTE YOUR PROJECT ID HERE
export const dataset = 'production'
export const apiVersion = '2024-01-01'
